namespace EvilHop
{
    /// <summary>
    /// Temporary marker type so the assembly has public surface to reference.
    /// Removed once the Block and Asset layers land.
    /// </summary>
    public static class Placeholder
    {
        /// <summary>
        /// Gets the library name.
        /// </summary>
        public static string Name => nameof(EvilHop);
    }
}
